<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= data.title || "Mission & Impact" %> | Mabuwaya Foundation</title>
  <link rel="stylesheet" href="/css/mission-impact.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  
  <style>
   
    /* ===== HOMEPAGE NAVBAR POSITION (sticky, exactly like homepage) ===== */
    
    
    .menu-btn {
      display: none;
      background: none;
      border: none;
      cursor: pointer;
    }
    .menu-btn .bar {
      display: block;
      width: 28px;
      height: 3px;
      background: white;
      margin: 6px 0;
    }

    /* ===== HERO TITLE (centered over background image) ===== */
    .hero-title {
      height: 90vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: white;
      text-shadow: 0 4px 15px rgba(0,0,0,0.6);
      padding: 0 20px;
    }
    .hero-title h1 {
      font-size: 5.5rem;
      font-weight: 900;
      margin: 0 0 20px 0;
      letter-spacing: 2px;
    }
    .hero-title p {
      font-size: 1.6rem;
      max-width: 800px;
      margin: 0;
      opacity: 0.95;
      line-height: 1.6;
    }

    /* ===== CONTENT CARD (white box over image) ===== */
    .content-card {
      max-width: 1000px;
      margin: -100px auto 80px;
      padding: 60px 70px;
      background: rgba(255, 255, 255, 0.97);
      border-radius: 20px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.2);
      backdrop-filter: blur(10px);
      position: relative;
      z-index: 10;
    }
    .content-card h2 {
      font-size: 2.6rem;
      color: #1a4d2e;
      margin: 0 0 40px 0;
      text-align: center;
      position: relative;
      padding-bottom: 15px;
    }
    .content-card h2::after {
      content: '';
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      bottom: 0;
      width: 100px;
      height: 5px;
      background: #f1c40f;
      border-radius: 3px;
    }
    .content-card p {
      font-size: 1.18rem;
      line-height: 1.9;
      color: #2c3e50;
      margin: 25px 0;
    }
    .content-card strong { color: #1a4d2e; }

    /* ===== FIVE PILLARS – Beautiful numbered list ===== */
    .pillars {
      counter-reset: pillar;
      padding: 0;
      list-style: none;
    }
    .pillars li {
      counter-increment: pillar;
      position: relative;
      padding-left: 80px;
      margin: 35px 0;
      font-size: 1.18rem;
      line-height: 1.8;
    }
    .pillars li::before {
      content: counter(pillar);
      position: absolute;
      left: 0;
      top: 0;
      width: 55px;
      height: 55px;
      background: #1a4d2e;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      font-weight: bold;
      box-shadow: 0 6px 20px rgba(26,77,46,0.3);
    }

    /* ===== HISTORY SECTION ===== */
    .history {
      max-width: 1000px;
      margin: 60px auto;
      padding: 50px 70px;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      text-align: center;
      font-style: italic;
      font-size: 1.3rem;
      line-height: 1.9;
      color: #333;
    }

    /* ===== ORIGINAL FOOTER (unchanged) ===== */
    .footer {
      background: linear-gradient(135deg, #1e2a38, #2c3e50);
      color: #f8f9fa;
      padding: 80px 8% 40px;
      margin-top: 100px;
    }
    .container { max-width: 1200px; margin: 0 auto; }
    .footer-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 40px;
      margin-bottom: 50px;
    }
    .footer-box h3 {
      color: #f1c40f;
      border-left: 4px solid #f1c40f;
      padding-left: 12px;
      margin-bottom: 20px;
    }
    .social-links a {
      color: white;
      font-size: 28px;
      margin-right: 18px;
      transition: all 0.3s;
    }
    .social-links a:hover { color: #f1c40f; transform: translateY(-6px); }
    .footer-bottom {
      text-align: center;
      color: #aaa;
      border-top: 1px solid rgba(255,255,255,0.1);
      padding-top: 30px;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      .hero-title h1 { font-size: 3.5rem; }
      .hero-title p { font-size: 1.3rem; }
      .content-card { 
        margin: -80px 5% 60px; 
        padding: 40px 30px; 
        border-radius: 16px; 
      }
      .content-card h2 { font-size: 2.2rem; }
      .pillars li { padding-left: 70px; }
      .pillars li::before { width: 48px; height: 48px; font-size: 1.5rem; }
      .history { padding: 40px 30px; margin: 40px 5%; }
      .menu-btn { display: block; }
      .nav-links {
        display: none;
        flex-direction: column;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background: rgba(44,62,80,0.98);
        padding: 20px 0;
      }
      .nav-links.active { display: flex; }
      .nav-links li { margin: 12px 0; text-align: center; }
    }
  </style>
</head>
<body>

  <!-- NAVBAR (your original) -->
  <header>
    <nav class="navbar">
      <h1 class="logo"><a href="/homepage"><%= data.siteName || "MABUWAYA FOUNDATION" %></a></h1>
      <ul class="nav-links" id="nav-links">
        <% (data.navLinks || []).forEach(link => { %>
          <li><a href="<%= link.href %>"><%= link.text %></a></li>
        <% }) %>
      </ul>

      <button class="menu-btn" id="menu-btn">
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
      </button>
    </nav>
  </header>

  <!-- HERO WITH BACKGROUND IMAGE -->
  <div class="hero-title">
    <h1><%= data.title || "Mission & Impact" %></h1>
    <p>Empowering communities to protect the Philippine crocodile and its habitat.</p>
  </div>

  <!-- MAIN CONTENT CARD -->
  <div class="content-card">
    <% if (data.intro) { %><p><%- data.intro %></p><% } %>
    <% if (data.focus) { %><p><%- data.focus %></p><% } %>
    <% if (data.smallPopulations) { %><p><%- data.smallPopulations %></p><% } %>
    <% if (data.mission) { %><p><%- data.mission %></p><% } %>

    <h2>Our Five Pillars</h2>
    <ol class="pillars">
      <% if (Array.isArray(data.pillars) && data.pillars.length) { %>
        <% data.pillars.forEach(function(item) { %>
          <li><%- item %></li>
        <% }) %>
      <% } else { %>
        <li>No pillars listed yet.</li>
      <% } %>
    </ol>
  </div>

  <!-- HISTORY SECTION -->
  <div class="history">
    <% if (data.history) { %>
      <p><%- data.history %></p>
    <% } else { %>
      <p>Our journey began with a shared vision to save the Philippine crocodile from extinction through community partnership and scientific conservation.</p>
    <% } %>
  </div>

  <!-- FOOTER (your original) -->
  <footer class="footer">
    <div class="container">
      <div class="footer-grid">
        <div class="footer-box">
          <h3>Our Office</h3>
          <p>San Mariano, Isabela<br>Philippines 3332</p>
        </div>
        <div class="footer-box">
          <h3>Phone</h3>
          <p>+63 917 123 4567<br>+63 78 123 4567</p>
        </div>
        <div class="footer-box">
          <h3>Email</h3>
          <p>info@mabuwaya.org<br>mabuwaya.foundation@gmail.com</p>
        </div>
        <div class="footer-box">
          <h3>Follow Us</h3>
          <div class="social-links">
            <a href="https://www.facebook.com/mabuwaya" target="_blank"><i class="fab fa-facebook-square"></i></a>
            <a href="https://x.com/mabuwaya" target="_blank"><i class="fab fa-twitter-square"></i></a>
            <a href="https://www.instagram.com/mabuwayafoundation/" target="_blank"><i class="fab fa-instagram"></i></a>
          </div>
        </div>
      </div>
      <p class="footer-bottom">© 2025 Mabuwaya Foundation. All rights reserved.</p>
    </div>
  </footer>

  <script>
    document.getElementById('menu-btn').addEventListener('click', () => {
      document.getElementById('nav-links').classList.toggle('active');
    });
  </script>
</body>
</html>